#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

# board is a global variable, can be used throughout


puts "Welcome to Tic Tac Toe!"

user_input = gets.chomp

index = input_to_index(user_input)

move(board,index)

display_board(board)